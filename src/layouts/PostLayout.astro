---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;
---

<Layout title="blog">
  <!-- ... -->
  <div class="flex flex-col text-xl gap-5 items-center justify-center">
    <div class="w-full">
      <img src={frontmatter.image.src} alt={frontmatter.image.alt} />
    </div>
    <article
      class="prose
      prose-h1:text-[34px] prose-h1:mb-[25px] prose-h1:font-extrabold
      prose-h2:text-[28px] prose-h2:font-bold
      w-full my-8"
    >
      <slot />
    </article>
    <!-- Markdown content is injected here -->
  </div>
  <!-- ... -->
</Layout>
